defaults: # http://jekyllrb.com/docs/configuration/#front-matter-defaults
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "pages"
    values:
      layout: "default"